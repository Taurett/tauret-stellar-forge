// Thin shim — sizes are now declared in src/lib/products.ts (the registry).
// Kept for backwards compatibility; new code should import from "@/lib/products".
export { getSizesFor } from "@/lib/products";
